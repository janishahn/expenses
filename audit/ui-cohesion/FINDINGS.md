# UI cohesion audit findings

Finding IDs are append-only. Evidence and statuses may be updated; findings are never deleted.

Status vocabulary: `open`, `fixed:<commit>`, `rejected:<reason>`, `approval-required`, `blocked:<signature>`.

| ID | File:line | Class | Evidence | Proposed remedy | Risk | Status |
| --- | --- | --- | --- | --- | --- | --- |
| F-0001 | `ui/src/assets/react.svg:1`; `ui/src/components/PageIntroAddButton.tsx:1`; `ui/src/components/ui/{badge,radio-group,scroll-area,select,separator,table,tabs}.tsx:1` | legacy-dead | TypeScript module-resolution traversal from `ui/src/main.tsx` plus every `ui/tests` root finds no import edge to any listed module. Repository-wide symbol/path search finds no external reference, route registration, dynamic import, selector, or asset URL. `react.svg` is the untouched Vite starter asset; the live favicon is `/expenses-mark.svg`. The global `tiptap-markdown.d.ts` also lacks an import edge but is explicitly included by `tsconfig.app.json` and is retained as compile-time reachability. | Delete the nine unreachable files in one dead-code batch; verify TypeScript, lint, build, and both primary browser projects remain unchanged. | Low: no runtime or test consumer exists, but deletion evidence must be rechecked immediately before applying. | open |
| F-0002 | `ui/src/components/ui/button.tsx:7`; `ui/src/components/ui/card.tsx:6`; `ui/src/components/ui/product-card.tsx:21` | legacy-dead | The only live `Button` consumers are `AppButton` and `ShellThemeQuickToggle`, both hard-code `variant=unstyled` and `size=legacy`; every stock button variant and size branch is unreachable. `Card` is consumed only by `AppCard` with `variant=unstyled`; the stock card variant and all six exported card subcomponents have zero imports. `AppCardLink` likewise has zero imports. Repository-wide symbol and prop searches find no test-only or dynamic consumer. | Prune unreachable variants, sizes, subcomponents, and `AppCardLink` while preserving the reachable base behavior and the canonical product wrappers; decide after the complete surface map whether the remaining one-branch bases add value. | Medium: these are shared foundations, so preserve exact DOM/classes/ref behavior for surviving consumers and verify every AppButton/AppCard surface. | open |
| F-0003 | `ui/src/components/ui/switch.tsx:20` | performance | The live canonical switch uses `transition-all`, so every animatable property is enrolled even though only the switch's intended color, border-color, box-shadow, and transform state changes should animate. The mandated UI review guidance explicitly requires narrow transition properties. | Replace `transition-all` with the exact transition-property utility required by the switch state change; keep timing and all state classes unchanged. | Low: class-only narrowing with no intended visual delta. | open |
| F-0004 | `ui/src/app/api-types.ts:57`; `ui/src/components/product/ProductSurfaces.tsx:5`; `ui/src/components/ui/dialog.tsx:30`; `ui/src/components/ui/product-card.tsx:8`; `ui/src/components/ui/sheet.tsx:30`; `ui/src/index.css:760` | legacy-dead | Whole-repository symbol/reference search finds an unused exported `DeletedTransaction` type (the deleted-transactions page defines its own route type), an unused `FinancialPanel` `message` role, unused `DialogTrigger`, `SheetTrigger`, and `SheetDescription` implementations, and an unused `AppCard` `soft` tone. A CSS selector-to-source/test/runtime audit additionally finds no consumer for `accordion-content`, `control-check-top`, `editor-rail`, `financial-panel-message`, `menu-transition`, `sparkline-shell`, `surface-card-soft`, `surface-list-shell`, `surface-section-body`, or `surface-section-header`; dynamically generated live panel/tone selectors and third-party Leaflet selectors were separately verified and excluded. | Remove the unreachable type, components/branches, exports, and selectors while preserving live internal portal/overlay helpers and dynamic/third-party selectors. | Low: every target has zero runtime, import, test, or dynamic consumer; rerun typecheck/build and representative overlays/surfaces. | open |
| F-0005 | `ui/src/components/IconPicker.tsx:265`; `ui/src/pages/RecurringRulesPage.tsx:561`; `ui/src/pages/TransactionDetailPage.tsx:218`; `ui/src/pages/TransactionsPage.tsx:965` | cohesion | Four live sites bypass or undermine an existing canonical wrapper: Icon Picker renders a raw `btn-inline`; recurring History links apply `btn-inline` directly; attachment actions request `tone="ghost"` and then override it with `btn-inline`; and the transaction register reproduces `FinancialPanel` classes directly. These are ordinary product actions/surfaces, not specialized semantic widgets. | Route each site through `AppButton tone="inline"` (using `asChild` for links), remove the conflicting attachment tone/class pairing, and render the register through `FinancialPanel role="ledger"` with identical classes/DOM semantics. | Low: consolidation should preserve rendered affordance and behavior exactly; test desktop and mobile attachment, recurring, icon-picker, and ledger paths. | open |
| F-0006 | `ui/src/components/IconPicker.tsx:233`; `ui/src/pages/ReportBuilderPage.tsx:379`; `ui/src/pages/ScenariosPage.tsx:492` | cohesion | The canonical `AppInput`, `AppNativeSelect`, `AppTextarea`, and `AppFieldLabel` field family is used across the product, but Icon Picker search, Report Builder notes, and the entire What If adjustment editor recreate ordinary fields with raw elements and partial `field`/`form-label` classes. This produces a visibly weaker field treatment and duplicates the product-field contract. Browser-special date/time/file inputs and dense native choice controls were reviewed separately and are not part of this finding. | Convert the ordinary text, month, select, textarea, and label sites to the canonical product-field wrappers without changing state, labels, input types, or handlers. | Medium: intended visual cohesion change across a highly conditional form; verify every adjustment type plus desktop/mobile report and category icon flows. | open |
| F-0007 | `ui/src/components/PageIntro.tsx:3`; `ui/src/pages/DigestPage.tsx:182`; `ui/src/pages/ForecastPage.tsx:214`; `ui/src/pages/InsightsPage.tsx:296`; `ui/src/pages/ScenariosPage.tsx:423` | cohesion | Four sibling analytical routes hand-roll the same title/description/update-status chrome while ordinary product routes use `PageIntro`. Their title sizing and spacing drift from the app convention, directly conflicting with the repository rule that repeated page chrome is a product convention. Auth pages, the admin elevation gate, and the shell-integrated Assistant title are materially distinct and intentionally excluded. | Add description support to `PageIntro` and migrate the four analytical headers, using the existing title accessory for update status. Preserve each route's copy and adjacent toolbar placement. | Medium: intentional typography/spacing unification; verify all four routes on desktop and mobile and audit title wrapping/loading status. | open |
| F-0008 | `ui/src/pages/AdminPage.tsx:249` | cohesion | Purge and rebuild success paths call blocking native `alert()`, while the adjacent recurring catch-up action already uses calm inline success/error feedback. Native browser chrome breaks the product feedback pattern and interrupts the workflow; neither mutation currently exposes an inline error. | Replace both alerts with the same local inline feedback model used by recurring catch-up, including narrow mutation errors and polite status semantics; add browser coverage for both outcomes. | Medium: operator-facing maintenance feedback changes, but the underlying mutations and confirmations remain unchanged. | open |
