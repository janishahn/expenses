import ChartSystemPrototype from "../ChartSystemPrototype"
import type { VariantProps } from "../types"

export default function MotionFirst(props: VariantProps) {
  return <ChartSystemPrototype {...props} mode="motion" />
}
